apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: hooni # Must live in the same namespace as your secret
spec:
  defaultCertificate:
    secretName: ionos-ssl-secret
